spring:
  datasource:
    username:  root
    password: Chandramma@1
    url: jdbc:mysql://localhost:3306/movies

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: "your_email"
    password: "your_code"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

project:
  poster: posters/     

  
base:
  url: "http://localhost:8080"